object Learn_Scala
{
    def main(args:Array[String]):Int =
    {
        //Q1 & Q2
        var k,i,j = 2
        var m,n = 5
        var (f,g) = (12.0f,4.0f);
        //var c = 'X'

        //Q3
        var (a, b, c, d) = (2,3,4,5);
        //var (k,g) = (4.3f,4.0f);

        println(k + 12 * m	);
        println(m / j);
        println(n % j);
        println(m / j * j);
        println(f + 10*5 +g);
        //println(++i * n); not support

        return 0;
    }
}